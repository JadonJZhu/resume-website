{
  "name": "resume-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:assets": "node ../tools/check-resume-assets.ts --public public src/content/projects",
    "check:types": "astro check",
    "test": "npm run check:assets && node --test \"src/**/*.test.ts\" && npm run check:types",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.14.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10"
  }
}
